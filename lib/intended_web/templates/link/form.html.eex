<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :expires %>
  <%= datetime_select f, :expires %>
  <%= error_tag f, :expires %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
